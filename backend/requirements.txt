fastapi==0.141.1
uvicorn[standard]==0.52.1
sqlalchemy==2.0.51
pydantic==2.13.4
pydantic-settings==2.15.0
email-validator==2.3.0
python-jose[cryptography]==3.5.0
bcrypt==5.0.0
python-multipart==0.0.32
numpy==2.2.6   # 3.11-compatible (2.5.x needs Python >=3.12)
requests==2.34.2
# torch (LSTM forecaster) is intentionally NOT installed on deploy — it OOMs the
# free-tier build (~800MB; the backend deploys to Railway, see railway.toml).
# app/ml/forecaster.py imports it lazily, so the /insights outlook & forecast
# routes degrade to "model unavailable" when it's absent. Install it separately
# (`pip install torch`) to enable them locally or on a larger plan.
